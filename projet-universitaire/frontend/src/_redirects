# Cloudflare - SPA Redirects Configuration
# Serve index.html for Angular client-side routing
# Only match paths that don't have a file extension (avoids infinite loop)
/assets/*  /assets/:splat  200
/*         /index.html     200!
